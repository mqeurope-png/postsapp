import type { IngestionProvider, PostCandidate, SearchInput } from '../types';
export class ExternalCollectorProvider implements IngestionProvider { providerName = 'external'; async search(input: SearchInput): Promise<PostCandidate[]> { const url = process.env.EXTERNAL_COLLECTOR_URL; if (!url) return []; const response = await fetch(url, { method: 'POST', headers: { 'content-type': 'application/json', ...(process.env.EXTERNAL_COLLECTOR_API_KEY ? { authorization: `Bearer ${process.env.EXTERNAL_COLLECTOR_API_KEY}` } : {}) }, body: JSON.stringify(input) }); if (!response.ok) throw new Error(`External collector failed: ${response.status}`); return response.json() as Promise<PostCandidate[]>; } }

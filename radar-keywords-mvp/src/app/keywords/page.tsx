import { prisma } from '@/lib/db';
async function createKeyword(formData: FormData) { 'use server'; await prisma.keyword.create({ data: { term: String(formData.get('term')), type: String(formData.get('type')) as never, language: String(formData.get('language')) as never, market: String(formData.get('market')) as never, sector: String(formData.get('sector')) as never, priority: String(formData.get('priority')) as never, frequency: 'manual', status: 'active' } }); }
async function runKeyword(formData: FormData) { 'use server'; const { runKeywordSearchJob } = await import('@/lib/ingestion/run-keyword-search-job'); await runKeywordSearchJob({ keywordId: String(formData.get('id')), providerName: 'mock', limit: 6 }); }
export default async function KeywordsPage() { const keywords = await prisma.keyword.findMany({ include:{_count:{select:{postKeywords:true}}}, orderBy:{createdAt:'desc'} }); return <main className="space-y-6"><h1 className="text-3xl font-bold">Keywords y hashtags</h1><form action={createKeyword} className="card grid gap-3 md:grid-cols-7"><input name="term" className="input" placeholder="#uvprinting" required/><select name="type" className="input"><option value="hashtag">hashtag</option><option value="keyword">keyword</option></select><select name="language" className="input"><option>ES</option><option>DE</option><option>FR</option><option>NL</option><option>EN</option></select><select name="market" className="input"><option>Spain</option><option>DACH</option><option>France</option><option>Benelux</option><option>Global</option></select><select name="sector" className="input"><option>UV</option><option>DTF</option><option>Laser</option><option>Packaging</option><option>Retail</option><option>Signage</option><option>Textile</option></select><select name="priority" className="input"><option>high</option><option>medium</option><option>low</option></select><button className="btn">Crear</button></form><div className="card overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-slate-400"><th>Término</th><th>Idioma</th><th>Mercado</th><th>Sector</th><th>Posts</th><th>Acción</th></tr></thead><tbody>{keywords.map(k=><tr key={k.id} className="border-t border-slate-800"><td className="py-3">{k.term}</td><td>{k.language}</td><td>{k.market}</td><td>{k.sector}</td><td>{k._count.postKeywords}</td><td><form action={runKeyword}><input type="hidden" name="id" value={k.id}/><button className="btn">Mock run</button></form></td></tr>)}</tbody></table></div></main>; }

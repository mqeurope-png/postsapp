'use client';
export function StatusForm({ postId, status }: { postId: string; status: string }) { async function change(value: string) { await fetch(`/api/posts/${postId}/status`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ status: value }) }); location.reload(); } return <select className="input" defaultValue={status} onChange={(event)=>change(event.target.value)}><option value="new">new</option><option value="reviewed">reviewed</option><option value="useful">useful</option><option value="discarded">discarded</option><option value="archived">archived</option></select>; }
